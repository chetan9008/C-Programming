#include<stdio.h>
#include<conio.h>
int main()
{
    int radius=2;
    printf("\nvalue without typecasting is %d",radius);
    printf("\nvalue with typecasting is %f",(float)radius);
    printf("\n%d",radius);
    return 0;
}